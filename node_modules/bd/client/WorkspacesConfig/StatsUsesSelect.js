
define( [

  "dojo/_base/declare"

, "put-selector/put"

, "hotplate/hotDojoDgridWidgets/StoreSelect"
, "hotplate/hotDojoStores/stores"
, "hotplate/hotDojoGlobals/globals"

], function(

  declare

, put

, StoreSelect
, stores
, globals

){

  return declare( [ StoreSelect ], {

    ownClass: 'workspaces-stats-uses-select',

    storeName: 'statsUses',
    storeParameters: { },
    //storeParameters: { workspaceId: globals.workspaceId },
    // query: { name: "O" },
    queryOptions: { placeNew: 'last' },
    renderRow: function( object ){
      var row = put('div.row', object.name );
      return row;
    },

  });
});


